query Character($id: ID!) {
  character(id: $id) {
    id
    name
    level
    experience
    background
    alignment {
      id
      name
    }
    # TODO: flatten personality into character
    personality {
      traits
      ideals
      bonds
      flaws
    }
    abilities {
      id
      name
      score
      modifier
    }
    skills {
      id
      name
      modifier
      isProficient
      ability {
        id
        name
      }
    }
  }
}

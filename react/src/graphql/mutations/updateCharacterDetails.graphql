mutation UpdateCharacterDetails($input: UpdateCharacterDetailsInput!) {
  updateCharacterDetails(input: $input) {
    id
    name
    level
    experience
    alignment {
      id
      name
    }
    background
    personality {
      traits
      ideals
      bonds
      flaws
    }
  }
}
